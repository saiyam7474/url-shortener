# URL Shortener

A small Spring Boot service that converts long URLs into short codes and
redirects users to the original URL.

## Tech Stack

- Java 25
- Spring Boot
- Maven
- Spring Data JPA
- H2 Database

## How to Run

Make sure Java 25 is installed.

Run the application using:

```bash
./mvnw spring-boot:run